/*
import * as types from '../actions/actionTypes';

export default function tileReducer(state = [],action){
	switch(action.type){
		case types.CREATE_TILE:
			return [...state,Object.assign({},action.tile)];
		default:
			return state;
	}
}

*/